{
  "name": "vitepress-plugin-sparql-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=vitepress-plugin-sparql",
    "dev:demo": "npm run build --workspace=vitepress-plugin-sparql && npm run dev --workspace=vitepress-plugin-sparql-demo"
  }
}
